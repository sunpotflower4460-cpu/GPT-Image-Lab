# Experiment 0001 assets

Expected primary output:

- `result.png` — the unedited GPT-Image-2 generation produced from `../experiment.json`

Do not retouch or replace the primary image before critique. If a corrected or edited variant is tested, give it a new filename and treat the change as new evidence rather than silently replacing the original.
